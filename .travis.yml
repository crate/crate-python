language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
  - pypy
  - pypy3
install:
  - pip uninstall -y setuptools
  - python bootstrap.py
  - ./bin/buildout -N
script:
  - bin/coverage run bin/test
after_success:
  - pip install coveralls
  - coveralls
notifications:
    email: false
